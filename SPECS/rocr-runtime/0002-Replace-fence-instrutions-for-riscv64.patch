diff -ruN ROCR-Runtime-rocm-7.2.4.orig/runtime/hsa-runtime/core/inc/amd_gpu_agent.h ROCR-Runtime-rocm-7.2.4/runtime/hsa-runtime/core/inc/amd_gpu_agent.h
--- ROCR-Runtime-rocm-7.2.4.orig/runtime/hsa-runtime/core/inc/amd_gpu_agent.h	2026-06-29 16:16:07.023158869 +0800
+++ ROCR-Runtime-rocm-7.2.4/runtime/hsa-runtime/core/inc/amd_gpu_agent.h	2026-06-29 16:16:07.039223450 +0800
@@ -439,9 +439,17 @@
   /// @brief Force a WC flush on PCIe devices by doing a write and then read-back
   __forceinline void PcieWcFlush(void *ptr, size_t size) const {
     if (!xgmi_cpu_gpu_) {
-      _mm_sfence();
+  #if !defined(__riscv)
+    _mm_sfence();
+#else
+    asm volatile("fence w,w" ::: "memory");
+#endif
       *((uint8_t*)ptr + size - 1) = *((uint8_t*)ptr + size - 1);
-      _mm_mfence();
+  #if !defined(__riscv)
+    _mm_mfence();
+#else
+    asm volatile("fence rw,rw" ::: "memory");
+#endif
       auto readback = *(reinterpret_cast<volatile uint8_t*>(ptr) + size - 1);
       UNUSED(readback);
     }
diff -ruN ROCR-Runtime-rocm-7.2.4.orig/runtime/hsa-runtime/core/runtime/amd_aql_queue.cpp ROCR-Runtime-rocm-7.2.4/runtime/hsa-runtime/core/runtime/amd_aql_queue.cpp
--- ROCR-Runtime-rocm-7.2.4.orig/runtime/hsa-runtime/core/runtime/amd_aql_queue.cpp	2026-06-29 16:16:07.023497034 +0800
+++ ROCR-Runtime-rocm-7.2.4/runtime/hsa-runtime/core/runtime/amd_aql_queue.cpp	2026-06-29 16:16:07.039420998 +0800
@@ -474,7 +474,11 @@
     HSAKMT_CALL(hsaKmtQueueRingDoorbell(queue_id_));
   } else {
     // Hardware doorbell supports AQL semantics.
+#if !defined(__riscv)
     _mm_sfence();
+#else
+    asm volatile("fence w,w" ::: "memory");
+#endif
     *(signal_.hardware_doorbell_ptr) = uint64_t(value);
     /* signal_ is allocated as uncached so we do not need read-back to flush WC */
   }
@@ -1558,7 +1562,11 @@
   memcpy(&queue_slot[1], &slot_data[1], slot_size_b - sizeof(uint32_t));
   if (IsDeviceMemRingBuf() && needsPcieOrdering()) {
     // Ensure the packet body is written as header may get reordered when writing over PCIE
+#if !defined(__riscv)
     _mm_sfence();
+#else
+    asm volatile("fence w,w" ::: "memory");
+#endif
   }
   atomic::Store(&queue_slot[0], slot_data[0], std::memory_order_release);
 
diff -ruN ROCR-Runtime-rocm-7.2.4.orig/runtime/hsa-runtime/core/runtime/amd_blit_kernel.cpp ROCR-Runtime-rocm-7.2.4/runtime/hsa-runtime/core/runtime/amd_blit_kernel.cpp
--- ROCR-Runtime-rocm-7.2.4.orig/runtime/hsa-runtime/core/runtime/amd_blit_kernel.cpp	2026-06-29 16:16:07.023518686 +0800
+++ ROCR-Runtime-rocm-7.2.4/runtime/hsa-runtime/core/runtime/amd_blit_kernel.cpp	2026-06-29 16:16:07.039523225 +0800
@@ -901,7 +901,11 @@
   std::atomic_thread_fence(std::memory_order_release);
   if (queue_->IsDeviceMemRingBuf() && queue_->needsPcieOrdering()) {
     // Ensure the packet body is written as header may get reordered when writing over PCIE
+#if !defined(__riscv)
     _mm_sfence();
+#else
+    asm volatile("fence w,w" ::: "memory");
+#endif
   }
 #if defined(__linux__)
   __atomic_store_n(&(queue_buffer[index & queue_bitmask_].full_header),
diff -ruN ROCR-Runtime-rocm-7.2.4.orig/runtime/hsa-runtime/core/runtime/intercept_queue.cpp ROCR-Runtime-rocm-7.2.4/runtime/hsa-runtime/core/runtime/intercept_queue.cpp
--- ROCR-Runtime-rocm-7.2.4.orig/runtime/hsa-runtime/core/runtime/intercept_queue.cpp	2026-06-29 16:16:07.023770683 +0800
+++ ROCR-Runtime-rocm-7.2.4/runtime/hsa-runtime/core/runtime/intercept_queue.cpp	2026-06-29 16:16:07.039593432 +0800
@@ -271,7 +271,11 @@
       ring[barrier & mask].barrier_and.completion_signal = Signal::Convert(async_doorbell_);
       if (wrapped->IsDeviceMemRingBuf() && needsPcieOrdering()) {
         // Ensure the packet body is written as header may get reordered when writing over PCIE
-        _mm_sfence();
+    #if !defined(__riscv)
+    _mm_sfence();
+#else
+    asm volatile("fence w,w" ::: "memory");
+#endif
       }
       atomic::Store(&ring[barrier & mask].barrier_and.header, kBarrierHeader,
                     std::memory_order_release);
@@ -318,7 +322,11 @@
       if (write_index != 0) {
         if (wrapped->IsDeviceMemRingBuf() && needsPcieOrdering()) {
           // Ensure the packet body is written as header may get reordered when writing over PCIE
-          _mm_sfence();
+      #if !defined(__riscv)
+    _mm_sfence();
+#else
+    asm volatile("fence w,w" ::: "memory");
+#endif
         }
         atomic::Store(&ring[write & mask].packet.header, packets[first_written_packet_index].packet.header,
                       std::memory_order_release);
@@ -419,7 +427,11 @@
 
     if (IsDeviceMemRingBuf() && needsPcieOrdering()) {
       // Ensure the packet body is written as header may get reordered when writing over PCIE
-      _mm_sfence();
+  #if !defined(__riscv)
+    _mm_sfence();
+#else
+    asm volatile("fence w,w" ::: "memory");
+#endif
     }
   }
   i = next_packet_;
